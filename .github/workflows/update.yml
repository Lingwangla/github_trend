name: Update GitHub Trending

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true

      - name: Update Trending and Pages
        env:
          GITHUB_TREND_TOPIC_NAME: ${{ vars.GITHUB_TREND_TOPIC_NAME }}
          GITHUB_TREND_KEYWORDS: ${{ vars.GITHUB_TREND_KEYWORDS }}
          GITHUB_TREND_EXTRA_KEYWORDS: ${{ vars.GITHUB_TREND_EXTRA_KEYWORDS }}
        run: go run ./cmd/trending

      - name: Commit generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add TRENDING.md index.html history_daily_trending

          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi

          git commit -m "chore: update github trending"
          git push

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Notify Feishu
        env:
          GITHUB_TREND_NOTIFY_ONLY: "1"
          FEISHU_NOTIFY_ENABLED: ${{ vars.FEISHU_NOTIFY_ENABLED }}
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_RECEIVE_ID: ${{ secrets.FEISHU_RECEIVE_ID }}
          FEISHU_RECEIVE_ID_TYPE: ${{ vars.FEISHU_RECEIVE_ID_TYPE }}
          FEISHU_PAGE_URL: ${{ steps.deployment.outputs.page_url }}
        run: go run ./cmd/trending
